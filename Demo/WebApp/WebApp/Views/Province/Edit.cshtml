@model WebApp.Models.Province
<form method="post">
    <div>
        <label>Area</label>
        <select name="areaId">
            <!--Cach 1-->
            @foreach (WebApp.Models.Area item in ViewBag.areas)
            {
                <option @(item.Id == Model.AreaId ? "selected" : null) value="@item.Id">@item.Name</option>
                @*@if (item.Id == Model.AreaId)
                    {
                        <option selected value="@item.Id">@item.Name</option>
                    }
                    else
                    {
                        <option value="@item.Id">@item.Name</option>
                    }*@
            }
        </select>
    </div>
    <div>
        <label>Name</label>
        <input type="text" name="name" value="@Model.Name" />
    </div>
    <div>
        <button>Save Changes</button>
    </div>
</form>